/**
 * /budget command handler
 */

export async function handleBudgetCommand(): Promise<string> {
  return 'Budget check is ready — I just need the budget system linked in.\n\nThis will show your limits, spent amounts, and what\'s left.';
}
